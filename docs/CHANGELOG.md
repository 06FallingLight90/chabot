# 更新日志

## 2026-08-29

> 本轮为**界面美化与现代化**改造（采用 **C 方案：wot-design-uni 按需引入 + 自建设计令牌系统**），纯端侧 UI 改造，**不改变任何业务逻辑、存储结构、记忆/聊天链路与对外接口**；核心逻辑测试（24 组记忆 + 8 组表情断言）全部保持通过。

### 新增
- **统一视觉基础（设计令牌）**：用一套「CSS 变量（`App.vue`）+ SCSS 变量（`uni.scss`）」定义品牌主色、135° 渐变（蓝→紫）、中性色、状态色、圆角、阴影、间距；所有页面引用同源令牌，改一处全局生效，保证跨页一致
- **wot-design-uni 按需引入**：通过 easycom `^wd-(.*)` 按需自动引入组件，只用上什么才打包什么，未用部分不增包体积；通过根节点覆盖 `--wd-color-*` 把组件库主题收敛为品牌色
- **手动浅色/深色主题**：新增「深色模式」手动切换（仅浅色/深色，默认浅色，**不跟随系统**），持久化 + 全局 mixin 即时换肤并同步原生导航栏配色；页面根节点绑定 `themeClass`，全部走令牌换肤
- **自绘底部导航栏**：新增 `custom-tab-bar.vue` 取代原生 tabBar——`pages.json` **彻底移除 `tabBar` 配置**（不再渲染原生栏、不预留原生占位），三页显式 `import` + `components` 注册（不依赖 easycom 自动扫描），用 `uni.reLaunch` 切页；用令牌配色，手动深浅色下三端底栏同步换肤
- **聊天气泡不透明度**：设置页新增滑块（0.2~1，步进 0.05），消息气泡改为「背景层 + 文字层」结构，背景透明度可调而文字始终清晰
- **设置页重构为首页 + 子页**：设置首页改为「分条陈列」的分区跳转入口（界面外观/接口配置/人格配置/拟真聊天/语音阅读/上下文压缩/数据管理/调试日志/帮助/关于），入口带说明与「保存/自动保存」标签，必要项未配置时显示红色 `*`；各子页独立 `navigateTo` 进入
- **接口配置小白引导**：免费额度模型推荐（通义千问/DeepSeek/Kimi/智谱 GLM/Ollama，点选自动填充地址+模型）与「查看傻瓜式配置教程」弹窗（面向零基础的图文步骤，含本地 Ollama 局域网接入）
- **关于页**：Logo/版本/简介/技术栈/接口协议/数据存储 + GitHub 仓库链接复制 + 鸣谢（koishi-ai-pet 记忆机制灵感）

### 优化
- **全页面令牌化适配深色**：聊天（头部/气泡/输入栏/表情栏/各弹窗）、记忆、表情、设置各页硬编码浅色全部替换为 `--c-*` 令牌，深色下不再出现白色弹窗/黑字气泡；用户气泡品牌渐变、助手气泡白卡片、顶栏品牌渐变 + 白字；设置子页共用 `.ss-*` 全局布局类
- **消息列表与上标重排等细节**：记忆/表情页加顶栏 hero 区 + 卡片化列表 + 圆角阴影；记忆筛选按钮改独占整行 flex 四等分 + `white-space: nowrap`，缩放不内部断字

### 变更
- **新增依赖**：`sass`（编译 scss 变量）、`wot-design-uni`（UI 组件库），运行前需 `npm install`
- **取消「跟随系统」深色**：App 端依赖 `prefers-color-scheme`/`uni.onThemeChange` 跨端不可靠，改为仅手动浅色/深色；删除 `darkmode`/`theme.json`/`themeLocation` 及程序化系统解析（原生导航栏由 `__applyNavbar` 按手动主题同步）
- **移除原生 tabBar 配置**：`pages.json` 不再声明 `tabBar`，底部导航完全交给自绘 `custom-tab-bar`（`uni.reLaunch` 切页）
- **版本号升级**：`manifest.json` 1.3.5 → 1.3.6

### 修复
- **设置页输入框深色黑字难读**：接口地址/Key 等输入框（`.ss-input`）与自定义人格文本域（`.custom-box`）补 `color: var(--c-text)`，深色模式下输入文字跟随浅色主题便于阅读

---

## 2026-08-28

### 新增
- **拟真聊天**：设置页新增「拟真聊天」区块（随会话快照，仅「现实时间」模式生效）——AI 会在随机时间节点主动给你发消息，模拟真人发消息节奏。可设置主动消息时段（起止小时，默认 9-23）与频率档位（低 45~120min / 中 15~45min / 高 5~15min 随机间隔）；支持「自定义倒计时（秒）」调试值，填秒数则固定按该倒计时触发（下限 10s 防误触 API），便于快速验证链路
- **拟真格式约束**：开启后该对话所有回复均受"每条消息 ≤1 句、连续表情 ≤2"约束（`countSentences` 按句末标点计句、连续标点合并），多条消息按换行拆分为独立气泡逐条展示（burst），不合格自动重试（上限 `maxRequestAttempts`）
- **主动消息发送链路**：前台单条链式 `setTimeout` 调度（非轮询、空闲零唤醒），退后台不清定时器（H5 隐藏标签页被节流后仍会触发，App 后台挂起时回前台自动补发）；到期门禁含时段窗口、会话已有用户消息、距上条用户消息 60 分钟内（超时视为用户已离开不打扰）、聊天页等待 LLM 回复期间不打扰
- **调度可观测**：设置页实时显示下次主动消息倒计时（每秒刷新）；每次重排写一条「拟真聊天调度」info 日志（含倒计时与档位）；「立即发送一条主动消息」调试按钮（忽略时段/间隔门禁，仅校验 API 配置）快速验证完整链路
- **话题收尾引导**：拟真模式下系统提示词新增 `PROACTIVE_GUIDE`，引导模型判断当前话题是否临近结束、用短句或 1 个表情收尾，避免强行续聊
- **后台系统通知（可选）**：新增 `utils/notify.js` 系统通知层——开启拟真时在设置页保存会自动申请系统通知权限（App Android 13+ `POST_NOTIFICATIONS` / H5 `Notification.requestPermission`），收到主动消息落库后**仅在应用处于后台时**弹系统通知（App `plus.push.createMessage` 本地通知 / H5 浏览器 `Notification`），前台用户在聊天页不打扰；小程序无任意系统通知能力静默跳过。App 真后台 JS 挂起、主动消息仅在回前台补发时发送，故 App 端后台弹通知当前基本不触发（H5 后台可正常弹）

### 修复
- **重新生成后 burst 前半段残留**：拟真模式一次回复拆为多条气泡，点击重新生成此前只删除最后一条气泡、前半段需切换页面重新加载才消失——现在按存储整段移除并同步重载展示
- **主动调度器失活**：曾因聊天页 loading 抑制期间到期不重排，导致调度器永久失效（倒计时一直"等待调度"，只能重启恢复）——调度重排只看功能开关（`_featureOn`：开关+现实时间+API 配置），与 loading 抑制解耦（抑制只拦发送不拦重排），任何时序下调度器都不会失效
- **回前台补发重复发送**：`catchUpProactive` 补发后残留的旧定时器会再次触发造成重复消息——补发前先清旧定时器（`_tick` 也以 `_dueAt` 非空为触发前提）防重复
- **0 延迟忙循环**：时段起点恰为当前小时时 `_nextDelay` 可能返回 0 导致 `setTimeout(0)` 忙循环——延迟恒 ≥60s（自定义倒计时 ≥10s）

### 优化
- **改设置即时生效**：新增 `rearmProactive()` 强制按当前会话设置重排（设置页保存/调试按钮、聊天页新对话/切换会话调用），`catchUpProactive()` 保持跨页不重置倒计时（定时器已挂载时跳过重排）
- **版本号升级**：`manifest.json` 1.2.4 → 1.3.0

## 2026-08-22

### 新增
- **消息语音阅读（TTS）**：设置页新增「语音阅读」区块，可开关 TTS 并配置 API Key、模型与音色（默认 `qwen3-tts-flash` / `Cherry`，附 Qwen-TTS 常用音色列表选择，音色支持手动输入自定义值）。开启后，LLM 新返回的消息在渲染时自动合成语音并播放：仅朗读文字、表情包占位不朗读；语音不落地成文件（直接播远程音频 URL），播放一次即销毁，不占用持久存储；同一时刻只播放一条，用户发送新消息 / 切换会话 / 离开页面时自动打断
- **TTS 接口测试**：语音阅读区块新增「测试语音接口」按钮，按当前配置发送一小段固定测试文本并尝试以设置音色播放，验证接口连通性 / API Key / 音色是否可用；测试的请求、响应、错误均写入调试日志

### 修复
- **TTS App 端偶发无声**：App 原生播放器在 `src` 刚设置后立即 `play()` 会因资源未就绪被静默忽略（无报错、不触发 onError）——App 端改为等 `onCanplay` 就绪后再 play，并 1s 兜底再 play 一次，确保资源就绪后正常出声
- **TTS 播放失败日志模糊**：App 端 uni 的 `onError` 仅回传 `errMsg='MediaError'`、不暴露错误码——尽力解析 `code`/`errorCode`/`message` 及原始结构写入日志（H5 端可映射到 1 中止 / 2 网络 / 3 解码 / 4 源不支持）；App 端拿不到具体码，改为**首次播放失败后自动重新合成一次并重试**（拿新 URL，兜住瞬时网络/解码问题），并写「TTS 播放失败，重新合成重试」日志
- **表情间空白产生空消息气泡**：模型在表情与消息/表情与表情之间插入空格、换行时，消息页会出现多个空气泡——`splitEmojiText` 现在跳过表情包前后的纯空白段并裁剪文本段首尾空白

### 优化
- **消息列表渲染性能**（表情多时 App 启动/滚动/发送卡顿的针对性优化）：① App/小程序上传表情时压缩图片（限宽 300px）降低渲染解码开销；② 消息列表启动仅渲染最近 150 条，上翻到顶部出现「加载更早消息」按钮、点击再加载 150 条（不做自动加载）；③ 消息行改用稳定 key（msgId + 段序号），新增消息时上方历史消息精确复用、不再成批重渲染表情图

## 2026-08-21

### 优化
- **表情包批量上传**：上传支持多选（最多 9 张），选图确认后逐张命名（弹窗显示进度 `x/n` 与图片预览），名称校验失败停留在当前张，全部完成提示「已添加 n 个表情」
- **表情栏拖拽排序**：长按表情 2 秒进入拖动模式，被拖表情略微放大并随触屏点移动，松手完成重排（插入位置之后的表情顺延一位）；短按发送、滑动滚动面板互不干扰
- **聊天服务模块化拆分**：`utils/chat.js` 由单一编排文件按业务域拆分为 4 个模块——`chat-settings.js`（设置域：全局默认 + 会话设置快照）、`chat-conversations.js`（会话域：新建/切换/删除/复制 + 历史展示）、`chat-compress.js`（压缩域）、`chat-state.js`（共享状态：memoryStore 单例 + 最近请求缓存，消除跨模块循环依赖）；`chat.js` 保留为统一门面，对外导出签名不变，App.vue / 各页面 / 测试的导入路径与调用零改动
- **聊天页组件化**：`pages/chat/chat.vue` 由 1446 行瘦身为页面骨架（约 308 行），拆出 7 个子组件（`pages/chat/components/`）——header（头部+情景条）/ msg-list（消息列表+侧边滑块+回到底部）/ input-bar（输入栏）/ emoji-panel（表情栏+拖拽排序+上传弹窗）/ scene-edit（情景编辑弹窗）/ history（会话历史弹窗）/ persona（人格设置弹窗）；页面仅持有会话状态并通过 props/$emit 协调子组件，样式随组件下沉为 scoped
- **文档同步**：更新 `CLAUDE.md` 目录结构与相关章节、`README.md` / `README_EN.md`

## 2026-08-20

### 新增
- **表情包功能**：全局共享的自定义表情列表，聊天输入框右侧新增「表情」按钮，点击在输入框下方展开表情栏（QQ/微信式网格展示），点击表情即插入 `$表情名$` 占位发送
- **表情管理**：新增「表情管理」页面（聊天页表情栏「管理」入口或上传后进入），支持上传图片表情、设置/修改表情名（必填、≤20 字、全局唯一、不含 `$`/换行）、删除表情；表情数量上限 50，H5 端自动压缩为小尺寸 PNG base64 防存储配额打满，App/小程序端 `uni.saveFile` 持久化
- **消息分条渲染**：消息中 `$表情名$` 占位自动渲染为对应表情图片，与文本分条独立显示（如 `你好 $小狗高兴$ 很高兴认识你` 渲染为"你好"/表情图/"很高兴认识你"三条）；未知表情名按原文文本显示
- **LLM 表情互动**：发送请求时把当前表情清单随 system prompt 注入，LLM 可在回复中穿插使用 `$表情名$`；回复中出现清单外（编造）的表情名判定格式不合格并自动重试

### 优化
- **表情栏过渡动画**：展开/收起带平滑的 `max-height` + 透明度过渡动画
- **自定义人格完整示例**：设置页与聊天页人格弹窗新增「查看完整示例」，可展开查看完整设定示例并一键填入编辑框；预设人格提示词支持展开查看具体内容
- **设置项「聊天表情包」**：人格设定区新增允许/禁用开关（默认允许），禁用后请求不携带表情清单，LLM 不会主动使用表情包（手动插入与渲染不受影响）

### 修复
- **唤起键盘输入栏掉底**：App 端聊天页配置 `softinputMode=adjustResize`；表情栏打开时唤起键盘，等键盘弹出（视口已压缩）后再关闭表情栏，输入栏直接从"表情栏上方"落到"键盘上方"，不再缩回屏幕底部被键盘遮挡
- **底部消息被遮挡**：表情栏/键盘升起后消息列表自动滚动对齐底部，最新消息始终可见
- **切换对话顶部栏不更新**：切换/删除会话后同步刷新顶部栏的人格名与模型名

## 2026-08-11

### 新增
- **回复格式校验与自动重试**：LLM 返回内容先经格式校验（须含对话文本、Scene 行有内容、Memory 行可解析），不合格自动重新请求，直到格式正确才展示并写入对话/记忆；设置页新增「最大请求次数」（1-20，默认 5）上限，达上限仍不合格则报错；重试与达上限均在调试日志记录

### 修复
- **伪 Memory 行漏检**：部分模型以自然语言前缀代替标准 `Memory:`（如 `✅ 更新：3. xxx | keywords:.. | importance:.. | level:..`），此前被当作普通对话文本放行。现检测"含 `| keywords:`/`| importance:`/`| level:` 结构但缺少 `Memory:` 前缀"的行，判定格式非法并触发重试
- **行内 Scene/Memory 标记漏检**：Scene/Memory 标记未独立成行（如 `正文 [Scene: xxx]`、`正文 Scene: xxx`），此前被当作普通对话文本放行。现检测非行首出现的 `Scene:`/`Memory:` 标记，判定格式非法并触发重试

## 2026-08-10

### 新增
- **记忆新建与多选删除**：记忆页新增「新建」按钮（内容 + 优先级 + 级别，直接新增一条不做相似度合并）；「多选」模式支持勾选多条记忆（含全选/取消全选）一键批量删除
- **导出聊天记录**：历史弹窗新增「导出对话为 .txt」——H5 直接下载（带 BOM 防乱码）；App 写入应用文档目录 `_doc` 并尝试系统打开；小程序降级为复制全文到剪贴板
- **中英文 README 文档**：新增 `README.md`（中文）与 `README_EN.md`（英文），涵盖功能、快速开始、Ollama 接入、目录结构与测试说明

### 修复
- **切页重载显示 Scene/Memory 标记**：回复落库此前保存的是**未清理的完整文本**（含 Scene/Memory 行），首次展示时手动剔除，但从其它页切回聊天页重新加载历史时标记会显示在气泡里。修复：① 落库改为清理后的文本，标记不再混入历史与上下文压缩；② `getHistoryForUI` 展示层兜底剔除行首 Scene/Memory 标记，旧数据同样不再显示

## 2026-08-08

### 新增
- **会话独立人格设置**：聊天页头部人格名可点击，弹出「当前对话人格」设置（4 个预置 + 自定义提示词），保存后仅作用于当前对话并写入会话快照；切换会话即切换对应人格，新会话自动快照全局人格

### 修复
- **Qwen3 等思考模型回复为空**：Ollama 兼容接口对思考型模型未指定参数时自动开启思考，且思考+回答全部写入 `message.reasoning`/`thinking` 字段、`content` 为空，导致 App 解析出空内容（token 消耗正常）。三层修复——① 新增「思考模式」设置（关闭/开启/跟随模型，默认关闭），请求体传 `reasoning_effort`（`none`/`high`）；② 服务端不识别该参数返回 400 时自动移除参数降级重试，兼容普通 OpenAI 服务；③ 解析层兜底：`content` 为空时读取 `reasoning`/`thinking` 字段展示，避免"请求成功却无返回"假象
- **Ollama 500 Jinja 模板错误（System message must be at the beginning）**：压缩概要此前作为**第二条 system 消息**插入 messages 中间，Ollama 等模板要求 system 必须在最前且只能一条。修复：压缩概要并入首条 system 末尾，请求始终只含一条位于开头的 system 消息

## 2026-08-07

### 新增
- **API 配置预设**：设置页接口配置区新增「预设配置」，可保存/覆盖/删除至多 3 套 API 配置（接口地址/API Key/模型/温度），点击预设一键填充并快速切换；「保存当前为预设」通过 ActionSheet 选择槽位，接口地址匹配供应商预设时自动取名
- **L2 情景记忆 = 约定清单（TODO）**：记忆指南明确 L2 主要维护与用户的约定/待办（如"约定今晚8点一起打游戏"），新约定立即新增、完成/取消/变化立即用修改/删除更新，保证清单始终反映最新约定
- **L3 临时记忆快速迭代**：记忆指南强化 L3 定位为近期琐事与临时状态，过时立即修改或删除旧项，不堆积过时信息
- **会话独立记忆与人格**：记忆、人格设置（人格/自定义提示词/情景时间模式）按会话独立存储，切换历史会话即切换对应记忆与人格
- **情景按会话独立**：情景历史（`scenes`）随会话独立，切换会话情景不串台；旧版全局情景首次启动自动迁移到首个会话
- **复制对话/记忆**：历史弹窗新增「复制对话到新会话」（消息+记忆+概要+人格+情景，标题带"副本"后缀）与「复制记忆到新会话」（仅记忆+人格快照），快速复用已有上下文

### 变更
- **记忆指南层级化**：`MEMORY_GUIDE` 由"触发条件"改为按 L1/L2/L3 分层说明用途与维护频率；修改/删除操作优先级高于新增
- **版本号升级**：`manifest.json` 1.0.6 → 1.0.7

### 修复
- **Ollama 本地模型无返回**：Ollama 的 `/v1/chat/completions` 兼容接口默认 `stream=true`（SSE 流式），与 OpenAI 默认非流式不同，导致 App 按非流式 JSON 解析不到内容。现请求体显式传 `stream:false`，强制 Ollama 返回标准 JSON，无需 App 支持流式解析；接口预设新增「Ollama(本地)」一键填入，响应格式异常日志附实际响应体便于排查

## 2026-08-05

### 新增
- **历史对话与会话管理**：对话存储从单流改为多会话模型（`chabot_conversations`），聊天页头部新增「新对话」「历史」入口；历史弹窗可切换会话、删除会话（删当前自动切最近），会话标题自动取首条用户消息，旧版单流数据首次启动自动迁移为第一个会话
- **上下文压缩**：将上文交由 LLM 压缩为精炼概要（保留关键事实/偏好/决定/进行中的事），后续请求只发送「概要 + 未压缩尾部」，精简上下文并减少 token 消耗；超 80 条自动分批、逐批合并概要；历史消息仍完整保留可翻阅
- **自动压缩间隔设置**：设置页可选 关闭/20/30/40/60/80 条，累计新增消息达到阈值后每轮回复末尾自动异步压缩（不阻塞回复展示）
- **调试日志面板**：设置页新增调试日志，记录每次 LLM 请求（完整 messages JSON）、响应（完整返回内容）、错误及关键操作（发送/会话/压缩/记忆/情景/保存设置），支持点击展开完整详情、刷新与一键清空；环形缓冲 200 条
- **情景历史记录**：情景存储改为最多 10 条 FIFO 历史，系统提示词注入「情景变化」序列，帮助 LLM 理解情景过渡，衔接更流畅
- **一键回到底部按钮**：聊天页右下角浮动按钮，仅当用户上翻离开底部时出现（`scrollTop` 差值 + 触摸方向双检测），点击动画滚回最新消息
- **情景历史查看**：情景编辑弹窗展示最近 10 条历史情景（最新在前），点击任意条目填入编辑框复用
- **压缩日志含完整概要**：调试日志中「压缩上文」条目详情附完整压缩后上文全文，可展开核对压缩结果

### 修复
- **日志展开无效**：收起态改用 JS 截断（前 200 字符预览）替代 `-webkit-line-clamp`（部分 WebView 无法释放截断），展开后可靠显示完整请求/返回内容
- **回到底部按钮点击无效**：`scroll-into-view` 仅在值变化时触发滚动，按钮复用时值未变导致静默失效；改为先清空再赋值
- **测试时序抖动**：记忆检索测试因同毫秒 `created_at` 并列导致"最近记忆"槽位选择不稳定，补回数据前加 10ms 延时使断言稳定

### 变更
- **对话存储键迁移**：对话从 `chabot_chat_history` 迁移至 `chabot_conversations`（会话数组）+ `chabot_active_conv`（当前会话 id）；旧键首次启动自动迁移，此后不再写入
- **重新生成走截断接口**：`popLastAssistant` 改用存储层 `truncateChat`，与多会话模型对齐
- **版本号升级**：`manifest.json` 1.0.5 → 1.0.6

## 2026-08-04

### 新增
- **重新生成按钮**：最新一条 AI 回复下方出现"重新生成"按钮，点击后移除该条回复并以相同输入重新请求 LLM，重生成同样走完整记忆/情景链路
- **情景描述去文学化**：情景指南改为中性事实记录（"深夜，用户在卧室准备考研"），禁止文学化描写
- **侧边滑块跟踪滚动位置**：滑块拇指现在实时跟随 scroll-view 滚动位置，不再仅在拖拽时显示
- **LLM 返回格式双向加固**：提示词新增反例对比（`Memory: user_fact: xxx` 等常见错误），解析层兼容全角→箭头、`category: content` 冒号分隔、Scene 行自动清洗引号/标点

### 修复
- **响应延迟问题**：三份内置指南（记忆/情景/对话规则）从 ~1200 字压缩到 ~335 字，每轮减少约 150 prompt tokens
- **滑块 thumb 常驻顶部问题**：~~新增 `@scroll` 事件 + `scrollRatio` 实时计算~~ App 端 scroll-view 的 `@scroll` 事件不返回 `scrollHeight`/`clientHeight`，导致 `scrollRatio` 恒为初始值。改为纯拖拽驱动：`dragRatio` 松手后保留在当前位置，发送新消息时自动重置为底部。移除 `@scroll` 和 `scrollRatio`
- **LLM 格式不规范导致漏解析**：Memory 行解析兼容 `category: content` 冒号格式和全角 `→` 箭头，Scene 行自动去除引号/末尾标点

## 2026-08-03

### 新增
- **记忆修改/删除操作**：LLM 可通过 `Memory: 修改 原内容 → 新内容` 和 `Memory: 删除 原内容` 格式修改或删除已有记忆，按内容逐字匹配
- **LLM 驱动情景记录**：每次对话 LLM 输出 `Scene:` 行记录当前情景（谁在哪做什么），聊天页顶部情景条展示，点击可查看/修改
- **情景时间模式**：设置中可选择"现实时间"（注入真实日期时间供情景判断）或"虚拟时间"（不发送时间，适合角色扮演）
- **侧边滑块**：聊天记录超过 15 条时右侧出现垂直滑块，按住拖动按比例快速定位到任意消息
- **手动编辑记忆级别**：记忆页编辑弹窗增加 L1/L2/L3 级别切换按钮，保存时沿用一致性兜底
- **聊天背景图片**：设置页可选择相册图片作为聊天背景，App端压缩后持久化到文件，H5端 canvas 压缩为 base64 避免 localStorage 配额超限

### 修复
- **持久化数据丢失**：App端 `plus.sqlite` 的 `openDatabase`/`selectSql` 为异步回调 API，与同步接口不匹配，导致启动时读取为空、后续全量重写清空数据。改为统一使用 `uni.setStorageSync` 同步存储，三端可靠
- **背景图不随聊天滚动**：背景从 `.page` 移到 scroll-view 可视区（`:style` 绑到 scroll-view），不随内容拉伸
- **背景图 H5 配额超限**：`uni.compressImage` 仅 App/小程序可用，H5 改用 canvas 压缩（限宽 1080px、JPEG 0.8）
- **记忆写入极少**：记忆指南从"只在重要新信息时"改为"宁多勿漏"的五类触发场景 + 输入→输出示例，LLM 遵循率大幅提升

### 优化
- **自定义人格字数扩容**：`textarea` 显式设置 `maxlength="5000"`，支持千字级人格提示词，新增字数统计
- **对话规则去保守化**：从"剩余不输出"改为"只有纯闲聊才跳过"，放宽记忆写入触发

### 变更
- **持久化层简化**：移除 `plus.sqlite` 分支（含全部条件编译代码），统一 `uni.setStorageSync`。历史 sqlite 数据因异步 API bug 从未正确读写，实际无数据丢失

---

## 项目初始化

- 基于 uni-app (Vue 3) 的轻量 LLM 人格聊天 App，无后端、纯端侧
- **记忆系统**（移植自 koishi-ai-pet）：SQLite 记忆持久化 → L1/L2/L3 三级分级、半衰期衰减、关键词+轻量文本相似度去重合并、召回冷却、MMR 分层检索、容量淘汰/L3过期清理/L2降级维护
- **LLM 驱动记忆**：system prompt 内置记忆指南，LLM 输出结构化 `Memory:` 行 → App 解析入库
- **人格系统**：4 个预设（温柔大姐姐/元气学妹/古明地恋/毒舌傲娇猫娘）+ 自定义
- **跨端兼容**：三页 tabBar（聊天/记忆/设置），App + H5 + 微信小程序
- 编写 `CLAUDE.md` 项目协作文档与 `npm test` 核心逻辑测试
